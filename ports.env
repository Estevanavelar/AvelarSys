# ========================================
# AvelarSys - Port Configuration Variables
# ========================================
# Este arquivo define as portas fixas para todos os módulos
# NÃO ALTERE estes valores sem consultar PORTS.md

# === FRONTENDS (3000-3009) ===
# Portas externas para frontends (sempre mapeiam para 3000 interno)
APP_PORTAL_PORT=3000
AVADMIN_FRONTEND_PORT=3001
STOCKTECH_FRONTEND_PORT=3002

# === BACKENDS (8000-8009) ===
# Portas externas para APIs e serviços backend
AVADMIN_BACKEND_PORT=8000
STOCKTECH_BACKEND_PORT=8002
WPPCONNECT_PORT=8003

# === INFRAESTRUTURA ===
# Portas do Nginx (sempre fixas)
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# === EXTERNOS (SUPABASE) ===
# Portas para serviços externos (não containers)
POSTGRES_PORT=5433
REDIS_PORT=6379

# === VALIDAÇÃO ===
# Script pode usar estas variáveis para validar configuração
EXPECTED_FRONTEND_PORTS="3000 3001 3002"
EXPECTED_BACKEND_PORTS="8000 8002 8003"
EXPECTED_INFRA_PORTS="80 443"
EXPECTED_EXTERNAL_PORTS="5433 6379"

# === MAPPING INTERNO ===
# Como os containers Docker mapeiam portas (externo:interno)
APP_PORTAL_MAPPING="${APP_PORTAL_PORT}:3000"
AVADMIN_FRONTEND_MAPPING="${AVADMIN_FRONTEND_PORT}:3000"
STOCKTECH_FRONTEND_MAPPING="${STOCKTECH_FRONTEND_PORT}:3000"
AVADMIN_BACKEND_MAPPING="${AVADMIN_BACKEND_PORT}:8000"
STOCKTECH_BACKEND_MAPPING="${STOCKTECH_BACKEND_PORT}:3000"
WPPCONNECT_MAPPING="${WPPCONNECT_PORT}:8003"
NGINX_MAPPING="${NGINX_HTTP_PORT}:80 ${NGINX_HTTPS_PORT}:443"